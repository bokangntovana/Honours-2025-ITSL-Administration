@model ITSL_Administration.Models.Course

@{
    ViewData["Title"] = "Course Details";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="form-header">
            <i class="fas fa-book-open me-2"></i>Course Details
        </h2>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>Back to List
        </a>
    </div>

    <div class="form-card p-4">
        <div class="row">
            <div class="col-md-6">
                <dl class="row details-list">
                    <dt class="col-sm-4 text-muted fw-bold">@Html.DisplayNameFor(model => model.ModuleID)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ModuleID)</dd>

                    <dt class="col-sm-4 text-muted fw-bold">@Html.DisplayNameFor(model => model.ModuleName)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ModuleName)</dd>
                </dl>
            </div>
            <div class="col-md-6">
                <dl class="row details-list">
                    <dt class="col-sm-4 text-muted fw-bold">@Html.DisplayNameFor(model => model.ModuleCredits)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ModuleCredits)</dd>

                    <dt class="col-sm-4 text-muted fw-bold">@Html.DisplayNameFor(model => model.ModuleDescription)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ModuleDescription)</dd>
                </dl>
            </div>
        </div>

        <div class="form-actions">
            <a asp-action="Edit" asp-route-id="@Model.ModuleID" class="btn btn-primary">
                <i class="fas fa-edit me-2"></i>Edit
            </a>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-list me-2"></i>View All Courses
            </a>
        </div>
    </div>
</div>
